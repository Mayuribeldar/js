//login


const {test,expect} = require("@playwright/test")

 
test("to check login page functionality", async({page})=> {

await page.goto("https://shopnewage.com/")

await page.$("")

expect().not.toBeNull()
await b.screenshot({path :""})

const count = await page.$$('ul li')
expect (b.length).toBe(200)
for (items of count){
  await   items.textContent()

  await text.trim().length().toBeGreaterThan(0)

.tobe()
.toBeGreaterThan()
.not.toBeNull()

await page.getAttribute('ul','role')

 

const count = await page.$$('ul li ul li')

for(const item of count){
const list = await item.textContent()

}






}
})