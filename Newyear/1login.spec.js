const {test} = require("@playwright/test")


test("to check login page with multiple correct and incorrect inputs to check the validation",async({page})=>{

await page.goto("https://shopnewage.com/account/login")

await page.lo

})